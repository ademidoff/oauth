{
  "name": "auth-server",
  "version": "1.0.0",
  "description": "Rewordly OAuth server",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.mjs",
    "dev": "esbuild plugin/code.ts --bundle --target=es6 --loader:.svg=text --outfile=plugin/dist/code.js --watch",
    "build": "esbuild plugin/code.ts --bundle --target=es6 --loader:.svg=text --outfile=plugin/dist/code.js"
  },
  "author": "Alex Demidoff",
  "license": "ISC",
  "dependencies": {
    "@figma/plugin-typings": "*",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
